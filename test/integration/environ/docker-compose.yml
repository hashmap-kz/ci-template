services:
  vm:
    build:
      context: .
      dockerfile: ./dockerfiles/sshd/Dockerfile
    image: pgrwl/sshd
    container_name: pgrwl_vm
    restart: unless-stopped
    ports:
      - "2525:22"
